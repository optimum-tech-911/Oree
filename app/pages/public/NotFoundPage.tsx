import { ArrowRight, SearchX, Sparkles } from "lucide-react";
import { ButtonLink } from "@/components/ui/Button";
import { usePageMeta } from "@/hooks/usePageMeta";

export default function NotFoundPage() {
  usePageMeta("Page introuvable", "Cette page n'existe pas ou a été déplacée.");
  return <section className="hero-grid surface-noise grid min-h-[820px] place-items-center overflow-hidden bg-[var(--night)] px-5 pb-20 pt-32 text-center text-white"><div className="relative z-10 max-w-4xl"><div className="relative mx-auto w-fit"><span className="absolute inset-0 rounded-[28px] bg-[var(--accent)] blur-2xl opacity-30" /><span className="relative grid size-18 place-items-center rounded-[25px] border border-white/12 bg-white/[.07]"><SearchX className="size-8 text-[color:var(--mint)]" /></span></div><p className="mt-8 text-xs font-bold uppercase tracking-[.18em] text-white/72">Erreur 404</p><h1 className="mt-4 text-balance text-[clamp(3.4rem,7vw,7.8rem)] font-semibold leading-[.9] tracking-[-.075em]">Cette page n'est pas <span className="gradient-text">dans le dossier.</span></h1><p className="mx-auto mt-6 max-w-xl text-base leading-8 text-white/72">Le Guide Orée peut retrouver une action avec quelques mots, ou vous pouvez revenir à l'entrée principale du parcours.</p><div className="mt-9 flex flex-col justify-center gap-3 sm:flex-row"><ButtonLink to="/" variant="dark" size="lg" arrow>Retour à l'accueil</ButtonLink><ButtonLink to="/diagnostic" variant="ghost" size="lg" className="border border-white/12 text-white hover:bg-white/[.08]">Démarrer le diagnostic <ArrowRight className="size-4" /></ButtonLink></div><div className="mt-12 inline-flex items-center gap-2 rounded-full border border-white/[.08] bg-white/[.035] px-4 py-2 text-xs text-white/72"><Sparkles className="size-3.5 text-[color:var(--mint)]" />Essayez le bouton Guide Orée en bas de l'écran</div></div></section>;
}
